import { useEffect, useState } from 'react';
import { api } from '../services/api';
import type { AuditResult, Memory, MemoryStrategy, ProviderOption, TargetProvider, TestCase, TestSuiteMode } from '../types/domain';
import { StepIndicator } from '../components/StepIndicator';
import { ResultDashboard } from '../components/ResultDashboard';
import { ConversationStep } from '../components/ConversationStep';
import { GroundTruthReview } from '../components/GroundTruthReview';
import { ModelComparison } from '../components/ModelComparison';
import { ExperimentStatistics } from '../components/ExperimentStatistics';
import { TestSuiteReview } from '../components/TestSuiteReview';
import { TargetMemoryTrace } from '../components/TargetMemoryTrace';

const sample = `[User] I used MySQL before, but the backend now uses PostgreSQL.\n[User] I generally prefer Python.\n[User] For the current ELEC5623 assignment, Java is required.\n[User] I am based in Sydney.\n[User] The assignment must use PostgreSQL rather than SQLite.`;
type PreparedRun = { runId: string; label: string; groupLabel: string };
type CompletedRun = { label: string; groupLabel: string; result: AuditResult };
type FailedRun = { runId: string; label: string; detail: string };

export function NewAudit() {
  const [step, setStep] = useState(0); const [text, setText] = useState(sample); const [consent, setConsent] = useState(false);
  const [conversationId, setConversationId] = useState(''); const [memories, setMemories] = useState<Memory[]>([]);
  const [strategies, setStrategies] = useState<MemoryStrategy[]>(['weak_first_hit']); const [targetProviders, setTargetProviders] = useState<TargetProvider[]>(['rule_based']);
  const [pipelineProvider, setPipelineProvider] = useState<TargetProvider>('rule_based'); const [evaluatorProvider, setEvaluatorProvider] = useState<TargetProvider>('rule_based');
  const [providers, setProviders] = useState<ProviderOption[]>([]); const [budget, setBudget] = useState(8); const [repetitions, setRepetitions] = useState(1); const [suiteMode, setSuiteMode] = useState<TestSuiteMode>('behavioural');
  const [runs, setRuns] = useState<PreparedRun[]>([]); const [results, setResults] = useState<CompletedRun[]>([]); const [failedRuns, setFailedRuns] = useState<FailedRun[]>([]); const [reviewTests, setReviewTests] = useState<TestCase[] | null>(null); const [runningLabel, setRunningLabel] = useState('');
  const [busy, setBusy] = useState(false); const [error, setError] = useState('');

  useEffect(() => { api.targetProviders().then(setProviders).catch(() => setProviders([])); }, []);
  const act = async (fn: () => Promise<void>) => { setBusy(true); setError(''); try { await fn(); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Something went wrong.'); } finally { setBusy(false); } };
  const submitConversation = () => act(async () => { const conversation = await api.createConversation(true, text); setConversationId(conversation.conversation_id); setMemories(await api.extract(conversation.conversation_id)); setStep(1); });
  const updateMemory = (memory: Memory, status: Memory['status'], canonical_value?: string) => act(async () => { const next = await api.updateMemory(memory.memory_id, { status, canonical_value }); setMemories((items) => items.map((item) => item.memory_id === memory.memory_id ? next : item)); });
  const addMemory = (canonicalValue: string) => act(async () => { const memory = await api.addMemory({ conversation_id: conversationId, canonical_value: canonicalValue }); setMemories((items) => [...items, memory]); });
  const confirm = () => act(async () => { const reviewed = await api.confirm(conversationId, memories.filter((memory) => memory.status === 'confirmed' || memory.status === 'edited').map((memory) => memory.memory_id)); setMemories(reviewed); setStep(2); });

  const selectedTargets = providers.filter((item) => targetProviders.includes(item.provider));
  const strategyOptions: Array<{ value: MemoryStrategy; label: string; description: string }> = [
    { value: 'weak_first_hit', label: 'Weak First-Hit', description: 'Uses only the first related memory. It does not resolve updates or conflicts.' },
    { value: 'strong_rule_based', label: 'Strong Rule-Based', description: 'Prioritises contextual requirements, updates, stable facts and superseded records.' },
    { value: 'strong_score_based', label: 'Strong Score-Based', description: 'Ranks related memories using transparent relevance, freshness and context weights.' },
    { value: 'scope_aware', label: 'Scope-Aware', description: 'Prioritises current project requirements for task prompts, and profile or preference memories only when the question asks for them.' },
  ];
  const selectedPipeline = providers.find((item) => item.provider === pipelineProvider); const selectedEvaluator = providers.find((item) => item.provider === evaluatorProvider);
  const toggleTargetProvider = (item: ProviderOption) => { if (!item.configured) return; setTargetProviders((current) => current.includes(item.provider) ? (current.length === 1 ? current : current.filter((provider) => provider !== item.provider)) : [...current, item.provider]); };
  const toggleStrategy = (strategy: MemoryStrategy) => setStrategies((current) => current.includes(strategy) ? (current.length === 1 ? current : current.filter((item) => item !== strategy)) : [...current, strategy]);
  const start = () => act(async () => {
    const plan = selectedTargets.flatMap((provider) => strategies.flatMap((strategy) => Array.from({ length: repetitions }, (_, index) => ({ provider, strategy, repetition: index + 1 }))));
    const experiment = await api.createExperiment({ conversation_id: conversationId, label: `Memory strategy comparison ${new Date().toISOString()}`, test_suite_configuration: { test_budget: budget, random_seed: 42, prompt_template_version: 'rule-based-v1', pipeline_provider: pipelineProvider, pipeline_model: pipelineProvider === 'rule_based' ? 'rule-based-v2' : selectedPipeline?.default_model, suite_mode: suiteMode } });
    const created = await Promise.all(plan.map(({ provider, strategy }) => api.createAudit({ conversation_id: conversationId, experiment_id: experiment.experiment_id, target_configuration: strategy === 'weak_first_hit' ? 'weak' : 'strong', memory_strategy: strategy, provider: provider.provider, model: provider.default_model, pipeline_provider: pipelineProvider, pipeline_model: pipelineProvider === 'rule_based' ? undefined : selectedPipeline?.default_model, evaluator_provider: evaluatorProvider, evaluator_model: evaluatorProvider === 'rule_based' ? undefined : selectedEvaluator?.default_model, temperature: 0, random_seed: 42, test_budget: budget, prompt_template_version: 'rule-based-v1' })));
    setRuns(created.map((run, index) => { const groupLabel = `${plan[index].provider.label} · ${strategyOptions.find((item) => item.value === plan[index].strategy)?.label}`; return { runId: run.run_id, groupLabel, label: repetitions > 1 ? `${groupLabel} · Run ${plan[index].repetition}` : groupLabel }; })); setStep(3);
  });
  const prepareTests = () => act(async () => { if (!runs[0]) throw new Error('No audit condition was created.'); setRunningLabel('Generating the shared test suite'); await api.generate(runs[0].runId); const suite = await api.testReview(runs[0].runId); setReviewTests(suite.tests); setRunningLabel(''); });
  const reviewTest = (test: TestCase, decision: 'accepted' | 'rejected') => act(async () => { if (!runs[0]) return; const changed = await api.reviewTest(runs[0].runId, test.test_id, decision); setReviewTests((items) => items?.map((item) => item.test_id === changed.test_id ? changed : item) ?? null); });
  const regenerateTest = (test: TestCase) => act(async () => { if (!runs[0]) return; const changed = await api.regenerateTest(runs[0].runId, test.test_id); setReviewTests((items) => items?.map((item) => item.test_id === changed.test_id ? changed : item) ?? null); });
  const readyForExecution = !!reviewTests?.length && reviewTests.every((test) => test.quality_status === 'accepted');
  const runAudit = () => act(async () => { if (!readyForExecution) throw new Error('Generate and complete the shared test-suite review before running the audit.'); const completed: CompletedRun[] = []; const failed: FailedRun[] = []; for (const run of runs) { setRunningLabel(run.label); try { await api.execute(run.runId); await api.evaluate(run.runId); completed.push({ label: run.label, groupLabel: run.groupLabel, result: await api.results(run.runId) }); } catch (cause) { failed.push({ runId: run.runId, label: run.label, detail: cause instanceof Error ? cause.message : 'This condition could not be completed.' }); } } setRunningLabel(''); setResults(completed); setFailedRuns(failed); if (!completed.length) throw new Error('No experimental conditions completed. Check the failed runs and retry them.'); setStep(4); });
  const retryFailedRuns = () => act(async () => { const recovered: CompletedRun[] = []; const stillFailed: FailedRun[] = []; for (const run of failedRuns) { setRunningLabel(run.label); try { const audit = await api.retry(run.runId); if (audit.status !== 'COMPLETED') throw new Error('The run is still incomplete. Try again after checking the provider configuration.'); const groupLabel = runs.find((item) => item.runId === run.runId)?.groupLabel ?? run.label; recovered.push({ label: run.label, groupLabel, result: await api.results(run.runId) }); } catch (cause) { stillFailed.push({ ...run, detail: cause instanceof Error ? cause.message : run.detail }); } } setRunningLabel(''); setResults((current) => [...current, ...recovered]); setFailedRuns(stillFailed); });

  return <main className="workflow"><StepIndicator current={step} />{error && <div role="alert" aria-live="assertive" className="alert">{error}</div>}
    {step === 0 && <ConversationStep text={text} consent={consent} busy={busy} onTextChange={setText} onConsentChange={setConsent} onImport={setText} onImportError={setError} onContinue={submitConversation} />}
    {step === 1 && <GroundTruthReview memories={memories} busy={busy} onChange={updateMemory} onAddMemory={addMemory} onConfirm={confirm} />}
    {step === 2 && <section><h1>Configure Memory Audit</h1><p>Select one or more memory strategies and target models. Every combination uses the same confirmed ground truth, frozen test suite and audit configuration.</p><h3>Controlled Memory Strategies</h3><div className="target-grid">{strategyOptions.map((item) => <label className={`target ${strategies.includes(item.value) ? 'selected' : ''}`} key={item.value}><input type="checkbox" checked={strategies.includes(item.value)} onChange={() => toggleStrategy(item.value)} /><strong>{item.label}</strong><span>{item.description}</span></label>)}</div><h3>Target Models to Compare</h3><div className="provider-grid">{providers.map((item) => <label className={`target ${targetProviders.includes(item.provider) ? 'selected' : ''}`} key={item.provider}><input type="checkbox" checked={targetProviders.includes(item.provider)} disabled={!item.configured} onChange={() => toggleTargetProvider(item)} /><strong>{item.label}</strong><span>{item.description}</span><small className={item.configured ? 'ready' : 'missing'}>{item.configured ? 'Available for comparison' : 'API key required'}</small></label>)}</div><p className="provider-note">Selecting one model with several strategies directly measures the effect of memory policy. Selecting several models compares both model and policy. API keys stay on the backend.</p><div className="form-grid"><label>Test suite design<select value={suiteMode} onChange={(event) => setSuiteMode(event.target.value as TestSuiteMode)}><option value="behavioural">Behavioural tests</option><option value="direct_ground_truth">Direct Ground-Truth baseline</option><option value="fixed_template">Fixed-Template baseline</option></select><small>Frozen once and shared by every condition.</small></label><label>Memory extraction and test generation<select value={pipelineProvider} onChange={(event) => setPipelineProvider(event.target.value as TargetProvider)}>{providers.map((item) => <option key={item.provider} value={item.provider}>{item.label}{item.configured ? '' : ' — key required'}</option>)}</select></label><label>Behaviour evaluator<select value={evaluatorProvider} onChange={(event) => setEvaluatorProvider(event.target.value as TargetProvider)}>{providers.map((item) => <option key={item.provider} value={item.provider}>{item.label}{item.configured ? '' : ' — key required'}</option>)}</select></label><label>Test budget<input type="number" min="1" max="100" value={budget} onChange={(event) => setBudget(Number(event.target.value))} /></label><label>Repeated runs per condition<input type="number" min="1" max="10" value={repetitions} onChange={(event) => setRepetitions(Math.max(1, Math.min(10, Number(event.target.value) || 1)))} /></label><label>Temperature<input value="0.0" readOnly /></label><label>Random seed<input value="42" readOnly /></label><label>Prompt-template version<input value="rule-based-v1" readOnly /></label></div><h3>Audit Dimensions</h3><p className="dimensions">Accuracy · Freshness · Conflict Resolution · Appropriate Use</p><button disabled={busy || selectedTargets.length === 0 || strategies.length === 0 || !selectedPipeline?.configured || !selectedEvaluator?.configured} onClick={start}>Start {selectedTargets.length * strategies.length * repetitions > 1 ? `${selectedTargets.length * strategies.length * repetitions}-Run Comparison` : 'Memory Audit'}</button></section>}
    {step === 3 && <section className="running"><h1>Prepare Memory Audit</h1><p>{runs.length > 1 ? `${runs.length} conditions will use one shared test suite.` : 'Generate and review the test suite before executing the controlled audit.'}</p>{busy && runningLabel && <p className="running-model">Currently working: <b>{runningLabel}</b></p>}<ul className="progress"><li>Preparing confirmed ground truth <b>Complete</b></li><li>Generating shared behavioural tests <b>{reviewTests ? 'Complete' : busy ? 'In progress' : 'Waiting'}</b></li><li>Researcher test-suite review <b>{readyForExecution ? 'Complete' : reviewTests ? 'Action required' : 'Waiting'}</b></li><li>Initialising target AI <b>{readyForExecution ? 'Ready' : 'Waiting'}</b></li><li>Executing memory tests <b>Waiting</b></li><li>Evaluating responses <b>Waiting</b></li></ul>{!reviewTests ? <button disabled={busy} onClick={prepareTests}>{busy ? 'Generating Shared Test Suite…' : 'Generate Tests for Review'}</button> : <><TestSuiteReview tests={reviewTests} busy={busy} onReview={reviewTest} onRegenerate={regenerateTest} /><button disabled={busy || !readyForExecution} onClick={runAudit}>{busy ? `Running ${runningLabel || 'audit'}…` : runs.length > 1 ? 'Run Comparison & View Results' : 'Run Audit & View Results'}</button>{!readyForExecution && <p className="provider-note">Accept each pending test and regenerate rejected tests before execution.</p>}</>}</section>}
    {step === 4 && results.length > 0 && <section><h1>{results.length > 1 ? 'Memory Health Model Comparison' : 'Memory Health Report'}</h1>{failedRuns.length > 0 && <div className="partial-run-warning"><b>{failedRuns.length} condition{failedRuns.length === 1 ? '' : 's'} did not complete.</b><ul>{failedRuns.map((run) => <li key={run.runId}>{run.label}: {run.detail}</li>)}</ul><button disabled={busy} onClick={retryFailedRuns}>{busy ? `Retrying ${runningLabel || 'run'}…` : 'Retry Failed Conditions'}</button></div>}{results.length > 1 && <><ModelComparison runs={results} /><ExperimentStatistics runs={results} /></>}{results.map((item) => <details className="individual-report" key={item.result.run_id} open={results.length === 1}><summary>{item.label} detailed report</summary><ResultDashboard result={item.result} /><TargetMemoryTrace runId={item.result.run_id} /></details>)}<button className="secondary" onClick={() => window.location.reload()}>Start New Audit</button></section>}
  </main>;
}
